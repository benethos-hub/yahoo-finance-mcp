version: 2
updates:
  # Python dependencies declared in pyproject.toml
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5

  # The actions the workflows use. They follow their moving major tag, so
  # fixes arrive without a pull request for every patch.
  #
  # astral-sh/setup-uv is the exception, and not by choice: it stopped
  # publishing major tags after v7. There is no v8, v9 or v10, only exact
  # versions, so it has to name one. That is also how this repository came to
  # sit three majors behind without noticing - `@v7` kept working and nothing
  # said anything newer existed. Dependabot is what notices now.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5

  # The base image in the Dockerfile. It was pinned to python:3.12-slim long
  # after 3.14 was out, and nothing said so: the image built, the container ran
  # and every gate stayed green on a base two releases behind. A stale base is
  # not a build failure, it is unpatched packages in something people pull.
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
